import SwiftUI

enum WidgetTheme {
    static let background = Color.black
    static let panel = Color(white: 0.12)
    static let textPrimary = Color(white: 0.92)
    static let textSecondary = Color(white: 0.7)
    static let textMuted = Color(white: 0.5)
}
